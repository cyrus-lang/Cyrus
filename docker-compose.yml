version: '3.9'

services:
  cyrus-dev:
    build:
      dockerfile: ./devel.Dockerfile
    container_name: cyrus-dev-container
    volumes:
      - .:/app
    working_dir: /app
    tty: true
    stdin_open: true
    restart: unless-stopped
