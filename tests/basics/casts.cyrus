// @stdout: a=42, b=4, c=8

import std::libc{printf};

fn main() {
    const a: int32 = 42;
    const b: int64 = a;

    const c: int64 = cast(int64, a); 

    printf("a=%d, b=%d, c=%d\n", a, sizeof(a), sizeof(c));
}
