// @stdout: 6 3
import std::libc{printf};

fn sum_two(x: int, y: int) int {
    return x + y;
}

fn sum_three(x: int, y: int, z: int) int {
    return x + y + z;
}

pub fn main() {
    printf("%d ", sum_two(1, 5));
    printf("%d\n", sum_three(1, 1, 1));
}
