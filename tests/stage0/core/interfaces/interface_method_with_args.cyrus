// @stdout: 120

import std::libc{printf};

interface Calculator {
    fn compute(self: const Self*, x: int32) int32;
}

struct Factorial : Calculator {
    pub fn compute(self: const Self*, x: int32) int32 {
        if (x <= 1) {
            return 1;
        }
        return x * self->compute(x - 1);
    }
}

pub fn main() {
    const calc: Calculator = dynamic Factorial{};
    printf("%d\n", calc.compute(5));
}
