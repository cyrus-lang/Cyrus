// @stdout: 1 2 3

import std::libc{printf};

pub fn main() {
    const untyped_array = {1, 2, 3};

    printf(
        "%d %d %d\n",
        untyped_array[0], 
        untyped_array[1],
        untyped_array[2]
    );
}