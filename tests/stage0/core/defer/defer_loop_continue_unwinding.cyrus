// @stdout: 1 3 5 DONE

import std::libc{printf};

pub fn main() {
    var i: int32 = 0;
    while (i < 6) {
        var current = i;
        i = i + 1;
        if (current % 2 == 0) {
            continue;
        }
        printf("%d ", current);
    }
    printf("DONE\n");
}
