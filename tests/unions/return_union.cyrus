// @stdout: Cyrus!

import std::libc{printf};

union Value {
    text_value: char*;
    int_value: int64;
}

fn new_text_value(text: char*) Value {
    return Value {
        text_value: text
    };
}

pub fn main() {
    const inst = new_text_value("Cyrus!");
    printf("%s\n", inst.text_value);
}