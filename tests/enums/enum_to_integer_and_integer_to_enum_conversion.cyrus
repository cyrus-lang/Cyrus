// @stdout: 2A

import std::libc{printf};

enum Tag {
	A = 1,
	B = 2,
	C = 3,
	D = 4
}

pub fn main() {
	var x = Tag.B;
	printf("%d ", cast(int32, x));

	x = cast(Tag, 1);

	switch (x) {
		case .A => {
			printf("A");
		}
		case .B => {
			printf("B");
		}
		case .C => {
			printf("C");
		}
		default => {
			printf("default\n");
		}
	}
}