// @stdout: Cyrus The Great

import std::libc{printf};

pub fn main() {
    const text = "Cyrus!";

    switch (text) {
        case "Cyrus!" => {
            printf("Cyrus The Great\n");
        }
        default => {
            printf("unknown\n");
        }
    }
}