// @stdout: 100

import std::libc{printf};

pub fn main() {
    var x: int32 = 0;

    @asm {
        "movl $50, %eax"
        "addl $50, %eax"
        "movl %eax, %0"
    } : "=r"(x) : : "eax";

    printf("%d\n", x);
}