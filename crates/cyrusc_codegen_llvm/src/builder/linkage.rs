use crate::builder::builder::IRBuilderCtx;
use inkwell::module::Linkage;

impl<'ll> IRBuilderCtx<'ll> {
    // fn emit_global_var_linkage(&self, vis: AccessSpecifier) -> Linkage {
    //     todo!()
    // }
}
