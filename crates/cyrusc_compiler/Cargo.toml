[package]
name = "cyrusc_compiler"
version = "0.1.0"
edition = "2024"

[dependencies]
buildmanifest = { path = "../cyrusc_buildmanifest", version = "*" }
scaffold_parser = { path = "../cyrusc_scaffold_parser", version = "*" }
inkwell = { version = "0.6.0", features = ["llvm18-1"] }
serde = { version = "1.0.219", features = ["derive"] }
