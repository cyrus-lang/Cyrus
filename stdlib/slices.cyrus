pub struct Slice {
	pub data: void*;
	pub length: size_t;
	pub element_size: size_t;

	pub inline fn index(self, idx: size_t) void* {
		var base = cast(uintptr, self.data);
		var offset = cast(uintptr, idx * self.element_size);
		var result = base + offset;
		return cast(void*, result);
	}
}