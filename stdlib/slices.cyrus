pub struct Slice {
	pub data: void*;
	pub length: usize;
	pub element_size: usize;

	pub inline fn index(self, i: usize) void* {
		var base = cast(uintptr, self.data);
		var offset = cast(uintptr, i * self.element_size);
		var result = base + offset;
		return cast(void*, result);
	}
}