pub struct Slice {
	pub data: void*;
	pub length: size_t;
	pub element_size: size_t;

	pub inline fn index(self, idx: size_t) void* {
		#base = cast(uintptr, self.data);
		#offset = cast(uintptr, idx * self.element_size);
		#result = base + offset;
		return cast(void*, result);
	}
}