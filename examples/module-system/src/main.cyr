import logger{Person};
// import std::libc;

func main() {
	// #a: Person;
	Person.hello_world();
	// logger::Logger.new("services.example");

	// libc::printf("value: %d\n", logger);
	// libc::printf("Group Name: %s\n", logger.group_name);
	// logger.greeting("Taha");
	// logger.logf("We believe in it.");
	// logger.errorf("No problem remains tho.");
}