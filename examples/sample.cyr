import std::libc{printf, malloc, strncpy, free, realloc};
import std::libc::abi;

public func sample() {
    printf("Hello, World!\n");
}

struct Sample {
    a: int;

    public func change(&self, new_a: int) {
        self->a = new_a;
    }
}
