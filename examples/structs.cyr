import std::io;

struct User {
    name: string;
    age: i32;

    pub fn do_something() {
        std::io::printf("Do Something\n");
    }

    pub fn info() {
        std::io::printf("Name: \n");
        std::io::printf("Age: \n");
    }
}

pub fn main() {
    #user = User {
        name: "Taha",
        age: 17
    };

    std::io::printf("name: %s\n", user.name);
    std::io::printf("age: %d\n", user.age);
}