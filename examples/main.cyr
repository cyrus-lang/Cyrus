extern func printf(format: const char*, ...);

struct Sample {
    public func sum(a: int, b: int) int {
        return a + b;
    }
}

func main() {
    printf("result: %d\n", Sample.sum(1, 2));
    // #hello = "Hello, ";
    // #world = "Cyrus!";
    // #combined = concat(hello, world);
    // printf("%s\n", combined);
}