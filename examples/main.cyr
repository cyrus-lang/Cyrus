import std::libc{printf, malloc};

enum DataType { 
    Int,
    Float,
    Text    
}

func main() {
    // #a = DataType.Int;
    // #b = DataType.Text;

    // a == b;

    // printf("value: %d\n", ptr[0]);
}