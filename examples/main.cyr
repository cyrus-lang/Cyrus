import std::libc{printf, malloc};

enum Color {
    Black,
    Purple,
    Custom(int, int, int)
}

func main() {
    #inst1 = Color.Black;
    #inst2 = Color.Black;

    // #res = inst1 == inst2;
    // printf("res: %d\n", res);
    if (inst1 == inst2) {
        printf("equal\n");
    } else {
        printf("not equal\n");
    }
}
