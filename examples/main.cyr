extern func printf(format: const char*, ...);

func main() {
    #a = 6uint;             // int (default, pointer-sized)
    #b = 3;             // int
    #x = a &~ b;        // bitwise AND-NOT: a & (~b)

    printf("%d\n", x);
}