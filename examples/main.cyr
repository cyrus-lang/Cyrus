import std::libc{printf, malloc};

enum DataType { 
    Int,
    Float,
    Text    
}

func main() {
    #a = DataType.Int;
    #b = DataType.Int;

    if (a == b) {
        printf("equal\n");
    } else {
        printf("not equal\n");
    }
}