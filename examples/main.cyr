extern func printf(format: const char*, ...) int32;

type Config = struct { host: char*, port: int };

config: const Config = struct {
	host = "127.0.0.1",
	port = 8080
};

func main() {
	printf("host: %s\n", config.host);
	printf("port: %d\n", config.port);
}