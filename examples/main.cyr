<<<<<<< HEAD
import std::libc{printf, malloc, free};

struct Person {
    id: uint;
    name: char*;
}

struct Organization {
    users: Person*;
}

func main() {
    #users_len: size_t = 2;
	#users: Person* = malloc(sizeof(Person) * users_len);
    users[0] = Person { id: 1, name: "Taha" };
    printf("%s %d\n", users[0].name, users[0].id);
    free(users);
}
=======

import std::libc{printf};

pub config = const struct {
    host = "127.0.0.1",
    port = 8080
};

fn main() {
    printf("%s %d\n", config.host, config.port);
}

// import std::libc{printf};

// fn main() {
//     #list = char*[3] {"Cyrus", "Programming", "Language"};

//     for (#x = 0; x < 5; x++) {
//         for (#i = 0; i < 3; i++) {
//             switch (list[i]) {
//                 case "Cyrus":
//                     printf("<%s/> ", list[i]);
//                     break;
//                 default:
//                     printf("%s ", list[i]);
//                     break;
//             }
//         }

//         printf("\n");
//     }
// }
>>>>>>> add_more_tests
