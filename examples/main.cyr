func main() {
    // #a: uint32 = 3 + 2;
    // #c: int32 = 2;

    // typecast(uint32, c);
    // #v = a + c;
    // printf("value: %d\n", v);

    #a: int*;
    #b: int* = null || a;
}

extern func printf(format: const char*, ...);