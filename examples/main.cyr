import sample;
import std::libc{printf};
import std::libc::abi{c_int};

func main() {
    #sample = sample::Sample{a: 10};
    #ptr = &sample;

    sample.change(5);

    printf("value: %d\n", ptr->a);
}