import std::libc;

func main() {
    #value: char* = "Cyrus";

    switch (value) {
        case "Taha":
            libc::printf("value A: %s\n", value);
            break;
        case "Cyrus":
            libc::printf("value B: %s\n", value);
        case "Rust":
            libc::printf("value C: %s\n", value);
            break;
        default:
            libc::printf("default condition\n");
    }
}
