import std::libc; 

func main() {
    #state = struct {
        name = "Cyrus",
        counter = 0,
    };

    state.counter += 1;
    state.counter += 1;
    state.counter += 1;
    libc::printf("counter: %d\n", state.counter);
}