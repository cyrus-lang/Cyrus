import std::io;

fn sqrt(value: double): double {
    return value;
}

pub fn main() {
    std::io::printf("Hello World\n");

    #result = std::math::sqrt(9);
    std::io::printf("result: %f\n", result);
}