func foo() char* {}

func main() {
    // #a = (closure)();              
    // #b = (foo() + bar());          
    // #d = (closure) == closure;     
    // #e = (closure);
    // #f = int32[3] {1, (closure + 1), 3};
    
    #c = typecast(const char*, foo()); 
}