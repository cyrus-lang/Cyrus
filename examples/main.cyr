import std::libc; 

enum Color {
    Red,
    Green,
    Valued = "my valued color!",
    Custom(int, int)
}

func main() {
    #variant = Color.Valued;

    switch (variant) {
        case .Red:
            libc::printf("Red\n");
            break;
    }
}