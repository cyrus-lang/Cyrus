extern fn printf(format: const char*, ...);
extern fn exit(status: int);

fn main() {
    printf("value: %d\n", fibonacci(1));
    // for (#i = 0; i < 30; i++) {
    //     printf("%d\n", fibonacci(i));
    // }
}
