public extern fn printf(format: const char*, ...);
public extern fn exit(status: int);

fn main() {
    for {
        printf("hello");
        continue;
    }

    // for (#i = 0; i <= 6; i++) {
    //     if (i == 2) {
    //         printf("i -> 3\n");
    //     } else {
    //         printf("exec once\n");
    //     }

    //     break;

    //     printf("should not be executed\n");
    // }
}