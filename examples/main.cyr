extern fn printf(format: const char*, ...): void;

fn main() {
    // Local struct
    // struct localModel {
    //     name: string;

    //     fn to_something() {
    //         printf("hello world\n");
    //     }
    // };

    // Nested unnamed struct
    #user = struct { 
        field1: int64 = 10,
        field2: int64 = 20,
    };

    // printf("name: %d\n", user.field2);
}

// #a: int32 = 10;
// #arr = int32[3] {a, 2, 3};
// printf("value: %d\n", arr[0]);