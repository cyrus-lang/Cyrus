import std::io;

struct User {
    name: string;
    age: i32;
}

fn main() {
    #user1 = User { 
        name: "Taha",
        age: 17
    };

    std::io::printf("%s\n", user1.name);
    std::io::printf("%d\n", user1.age);
}