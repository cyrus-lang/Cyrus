extern fn printf(format: const char*, ...);

fn main() {
    #a = 10;
    #value: void* = &a;
    #intptr_value = (uintptr) value;

    printf("value: %x\n", intptr_value);

    #pointer = (int*) intptr_value;
    *pointer = 200;
    
    printf("value: %d\n", *pointer);
}