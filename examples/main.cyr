extern fn printf(format: const char*, ...);
extern fn exit(status: int);

struct Person {
    public fn display(name: string) {
        self.name;
        printf("Hello, %s\n", name);
    }
}

fn main() {
    #user = Person { };

    user.display("Taha");
}