import std::io;

struct User {
    name: string;
}

fn sample(fmt: string, name: string) {
    std::io::printf(fmt, name);
}

fn main() {
    #user1 = User { 
        name: "Taha",
    };

    sample("%s\n", user1.name);
}