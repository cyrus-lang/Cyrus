extern fn printf(format: const char*, ...);

fn internal_bool_to_string(value: bool): string {
    if (value == true) {
        return "true";
    } else {
        return "false";
    }
}

fn main() {
    #fmt = internal_bool_to_string(true);

    printf("value: %s\n", fmt);
}