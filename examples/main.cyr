// extern func printf(format: const char*, ...);

type Bool = bool;
type Int = int;

func sample(x: Int, y: Bool) Int {
    return 0;
}

// func cmp(x: Int, y: Int) Bool {
//     // return x == y;
//     return true;
// }

func main() {
    // #v: Bool = true;
    // #a = sample();
    // #c: Int = 0;
    
    // #c: Bool;
    // c = sample();
}
