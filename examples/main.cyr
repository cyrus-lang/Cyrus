import std::libc{printf};

MAX_SIZE: const int = 4;

struct Bar {
    a: int[MAX_SIZE];
}

func main() {
    #bar: Bar;
//   #instance = Bar {
//     a: int[3] {},
//   };
    printf("v: %d\n", (bar.a)[MAX_SIZE - 1]);
}