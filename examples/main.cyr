struct Owner {
    name: string;
    age: i32;
}

fn main() {
    #owner = Owner {
        name: "Taha",
        age: 18
    };

    println("Name: %s\n", owner.name);
    println("Age: %d\n", owner.age);
}