extern func printf(format: const char*, ...);

func main() {
    // #i = 0;
    // printf("value: %d, %d, %d\n", i++, ++i, i);

    for (#i = 0; i < 10; i++) {
        // if (i >= 6) {
        //     continue;
        // } else {
        // }
        printf("i: %d\n", i);
    }
}
