import std::libc; 

struct Sample {
    name: char*;
}

union MyUnion {
    a: int;
    b: char*;
    c: Sample;

    public func get_a(&self) {
        libc::printf("value: %d\n", self->a);
    }
}

func main() {
    #un: MyUnion;
    un.a = 10;

    un.get_a();
}