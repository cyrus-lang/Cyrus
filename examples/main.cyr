import std::libc;

func main() {
    struct Sample {
        a: int;
        b: int;
    }

    #obj = Sample {
        a: 10,
        b: 20,
    }; 

    libc::printf("value: %d\n", obj.a);
    libc::printf("value: %d\n", obj.b);
}