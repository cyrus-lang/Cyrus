extern fn printf(format: const char*, ...): void;

struct Person {
    name: string;
    age: int;
}

fn main() {
    // Nested unnamed struct
    #user = struct { 
        metadata = struct {
            uuid = 1,
            role = "Admin",
            description = "This is my fuckin' programming language!!!"
        },
    };

    printf("uuid: %d\n", user.metadata.uuid);
    printf("role: %s\n", user.metadata.role);
    printf("description: %s\n", user.metadata.description);
}