// extern func printf(format: const char*, ...);

type Int = int;
type Bool = bool;

func cmp(x: Int, y: Int) Bool {
    // return x == y;
    return true;
}

func main() {
    // #b = MyMyPerson {};
}
