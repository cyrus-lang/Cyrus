extern fn printf(format: const char*, ...);

fn sum(static: int, vargs: ...int) {
    #value = vargs[0];

    // foreach (item in vargs) {
    //     printf("%d\n", item);
    // }
}

fn main() {
    printf("Hello Cyrus!\n");
    sum(1, 2, 3, 4, 5);
    //  ^
    //  static 
    //     ^
    //     vargs
}