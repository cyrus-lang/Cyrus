import sample;

func main() {
    #person = sample::MyPerson { name: "Taha", age: 10 };
    printf("MY PI: %f\n", sample::PI);
    
    person.display();
    
    sample::sample();
    sample::hello();
    sample::hello();
}

extern func printf(format: const char*, ...);