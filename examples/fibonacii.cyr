extern fn printf(format: const char*, ...);

fn fibonacci(n: int): int64 {
    if (n <= 1) {
        return n;
    }

    return n + 1;
}

fn main() {
    for (#i = 0; i <= 6; i++) {
        printf("%lld\n", fibonacci(i));
    }
}