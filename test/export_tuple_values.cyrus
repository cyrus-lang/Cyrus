// @stdout: (10, 20)
// @stdin: 
// @args:

import std::libc{printf};

fn main() {
    var (x, y) = pair(10, 20);

    printf("(%d, %d)\n", x, y);
}

fn pair(x: int, y: int) (int, int) {
    return (x, y);
}