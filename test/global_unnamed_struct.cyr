// @stdout: (127.0.0.1, 8080)

import std::libc{printf};

struct Config {
    pub host: char*;
    pub port: uint;
}

pub config = const Config {
    host: "127.0.0.1",
    port: 8080
};

fn main() {
    printf("(%s, %d)\n", config.host, config.port);
}