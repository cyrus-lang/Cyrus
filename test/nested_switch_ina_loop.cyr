// @stdout: <Cyrus/> Programming Language
// @stdin: 
// @args:

import std::libc{printf};

fn main() {
    var list = char*[3] {"Cyrus", "Programming", "Language"};

    for (var i = 0; i < 3; i++) {
        switch (list[i]) {
            case "Cyrus":
                printf("<%s/> ", list[i]);
                break;
            default:
                printf("%s ", list[i]);
                break;
        }
    }
}